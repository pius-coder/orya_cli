# Orya v2 — agent (LangGraph) requirements

# Web framework
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.11.5
python-dotenv>=1.0
httpx>=0.27

# Async PG
asyncpg>=0.29

# LangChain stack (multi-provider)
langchain>=0.3.27
langchain-core>=0.3.55
langchain-openai>=0.2.14
langchain-groq>=0.2.4

# LangGraph orchestration + Postgres checkpointer
langgraph>=0.5
langgraph-checkpoint-postgres>=2.0
psycopg[binary,pool]>=3.2

# Graphiti (knowledge graph) with Neo4j driver
graphiti-core>=0.11

# LangSmith (auto-traces LangChain & LangGraph when env vars set)
langsmith>=0.2

